# Backend API base URL (no trailing slash). Leave empty in local dev to use the Vite proxy.
VITE_API_BASE_URL=

# Supported alias for VITE_API_BASE_URL
# VITE_API_URL=

# Optional override for the Vite dev-server API proxy target
# VITE_API_PROXY_TARGET=
