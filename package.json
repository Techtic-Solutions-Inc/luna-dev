{
  "name": "agentwise",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "my-app"
  ],
  "scripts": {
    "dev": "npm run dev -w my-app",
    "build": "npm run build -w my-app",
    "lint": "npm run lint -w my-app",
    "typecheck": "npm run typecheck -w my-app",
    "test": "npm run test -w my-app",
    "preview": "npm run preview -w my-app"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
