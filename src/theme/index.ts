import { tokens, type Theme } from './tokens';

export const theme: Theme = tokens;

export { GlobalStyle, AppThemeProvider, ThemeProvider } from './ThemeProvider';
export { tokens } from './tokens';
export { breakpoints } from './breakpoints';
